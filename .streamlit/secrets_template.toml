# Copy to .streamlit/secrets.toml and fill in:
# Option A: Public/SAS URL (recommended)
DATA_URL = "https://<account>.blob.core.windows.net/traffic-data/traffic_with_time.parquet?<SAS>"

# Option B: Private Blob with SDK (only if you won’t use SAS)
# AZURE_STORAGE_ACCOUNT = ""
# AZURE_STORAGE_KEY = ""
# AZURE_CONTAINER = "traffic-data"
# AZURE_BLOB = "traffic_with_time.parquet"
